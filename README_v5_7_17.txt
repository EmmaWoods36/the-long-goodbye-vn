# The Long Goodbye — V5.7.17 Index + Assets + Manifest Package

This is the package you asked for: updated index, README, assets folder, and asset manifest.

## Included

- `index_story_polish_v5_7_17.html`
  - Updated index file.
  - Built from the verified V5.7.16 index mapping pass.

- `assets/`
  - Updated image assets from the uploaded subfolder zip files plus the standalone Tommy family dinner image.
  - This is a patch asset folder, not a full archive of every original image ever used by the project.

- `asset_manifest_v5_7_17.csv`
  - Lists every image included in this package's `assets/` folder.
  - Includes filename, dimensions, file size, whether the current index directly references it, and available mapping/source notes.

- `index_asset_references_not_in_patch_folder_v5_7_17.csv`
  - Lists any `assets/...` paths referenced by the index that are not included in this patch folder.
  - Those are expected to remain in your existing project assets folder if they were original/pre-existing files.

- Mapping / inventory CSVs
  - Included for traceability from the asset patch passes.

## How to install

1. Unzip this package.
2. Put `index_story_polish_v5_7_17.html` in your project root.
3. Copy/merge the included `assets/` folder into your existing project `assets/` folder.
4. Allow overwrite when prompted.
5. Open/deploy `index_story_polish_v5_7_17.html`.

## Expected folder structure

```text
project-folder/
  index_story_polish_v5_7_17.html
  assets/
    ...
```

## Notes

- The Tommy eating-with-family standalone image is included and mapped.
- The Tommy Secret Ending and Tommy ULTRA Secret Ending zip images are included and mapped.
- Route card, menu, route, interruption, ending, and CG patch assets from the uploaded zip batches are included.
- This package avoids bundling a separate archive of unrelated original assets.

Generated: 2026-06-11 14:17
